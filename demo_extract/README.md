Demo: Extraction with 7-Zip

Files:
- `extract.bat` - Extracts an archive to `extracted` folder by default.

Usage:
```
extract.bat               (uses ../demo_compress/sample.7z)
extract.bat archive.7z out_folder
```
