Demo: Searching archives for filenames

Files:
- `search_by_name.bat` - Searches `.7z` archives in `../demo_compress` for a filename pattern.

Usage:
```
search_by_name.bat secret
```
Note: Ensure `7z` is available on PATH or set `7ZIP` env var. The script looks for `.7z` files produced by the compression demo.
