Demo: Hashing files (Windows)

Files:
- `hash.bat` - Computes SHA256 using `CertUtil` (built-in on Windows).

Usage:
```
hash.bat                  (uses ../demo_compress/sample.7z)
hash.bat file_or_archive
```
